# __PLUGIN_NAME__

Sample plugin created with `commerce plugin create`.

## Commands

```bash
dotnet build
commerce plugin validate
commerce plugin pack
```

## Manifest

Update `Plugin.json` with your plugin metadata and keep `systemName` aligned with `CommercePluginSystemName` in the project file.
